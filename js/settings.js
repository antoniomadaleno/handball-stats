// ═══════════════════════════════════════════
// settings.js — definições da época
// ═══════════════════════════════════════════

import { S } from './state.js';
import { DB } from './db.js';
import { toast } from './utils.js';

export function renderSettings() {
  // placeholder — será preenchido com as definições
}